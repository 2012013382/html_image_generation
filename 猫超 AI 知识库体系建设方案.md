# 猫超 AI 知识库体系建设方案

## 1. 建设目标

我们要建的不是传统文档库，而是支撑超喵、章鱼和专家 Agent 正常运作的**经营知识底座**。

目标是让 AI 能够：

```text
懂业务规则
理解业务场景
调用正确经验
知道权限边界
支持任务托管
在使用中持续进化
```

一句话：

> 把猫超分散在业务专家、文档、系统、任务和复盘里的经验，沉淀成 AI 可理解、可使用、可治理、可持续更新的知识体系。

---

## 2. 整体方案

整体链路建议用一张图表达：

```text
知识获取
  冷启动导入 / 主动学习 / 任务回流 / AAR复盘
        ↓
AI 经验萃取
  按 PD 定义的场景知识标准，萃取成规则、SOP、策略、案例、诊断树
        ↓
知识组织与分类
  业务域 / 专家Agent / 场景 / 流程节点 / 知识类型 / 风险等级
        ↓
知识权限治理
  管理权限 + 使用权限 + 商家披露 + Agent执行边界
        ↓
Agent 使用知识
  按任务、角色、权限、场景召回知识，生成策略和执行建议
        ↓
知识更新
  通过冲突检测、用户纠错、任务效果、复盘结果持续更新
        ↓
产技业共建
  业务供给经验，产品定义结构，技术建设平台和自动化能力
```

---

# 3. 分模块方案

## 3.1 知识获取

### 目标

解决知识从哪里来，以及业务同学如何低成本参与。

### 来源与链路

知识获取分四类：

```text
1. 冷启动导入
   专家文档、SOP、规则、历史复盘、会议纪要、产品方案

2. 主动学习沉淀
   业务同学和 AI 多轮对话，由 AI 追问并沉淀隐性经验

3. 任务过程回流
   从小二/商家使用 Agent 的对话、修改、确认和执行结果中沉淀经验

4. AAR 复盘沉淀
   从策略效果好坏中复盘，更新策略、规则和案例
```

### AI 经验萃取

业务专家不需要按复杂模板写知识。  
正确链路是：

```text
业务投喂资料
-> AI 识别资料类型
-> AI 萃取规则、SOP、策略、案例
-> AI 判断缺失项和完整度
-> PD/业务 owner 确认关键内容
-> 进入知识库
```

这里 PD 的作用不是写业务知识，而是定义：

```text
某个业务场景下，Agent 要完成任务需要哪些知识
一条完整策略必须包含哪些字段
哪些缺失项不能发布
哪些内容需要业务确认
```

---

## 3.2 知识更新

### 目标

解决知识过时、冲突、失效的问题。

### 冲突检查方式

系统需要通过 AI 和使用数据识别冲突：

```text
新经验和旧经验冲突
不同专家观点冲突
策略和规则冲突
不同业务域知识冲突
Agent 使用后被用户频繁纠错
任务复盘发现旧策略失效
```

### 冲突解决路径

不同冲突走不同处理方式：

```text
事实冲突：由业务 owner 裁决
规则冲突：以正式制度和最新版本为准
策略冲突：拆分适用场景，不强行合并
跨域冲突：主责 PD 组织相关业务负责人会签
高风险冲突：进入专项审核和版本发布
```

知识更新不是直接覆盖旧知识，而是：

```text
生成新版本
-> 标记差异
-> 审核确认
-> 发布新版本
-> 旧版本降权或废弃
```

---

## 3.3 知识组织和分类

### 目标

让知识既能被业务维护，又能被 Agent 正确使用。

### 多维分类

知识不能只按一个目录组织，需要多维分类：

```text
1. 业务域
   解决谁负责维护，例如营销、招商、商品、供应链、风控等

2. 专家 Agent
   解决哪个 Agent 使用，例如招商 Agent、采购 Agent、营销 Agent

3. 业务场景
   解决什么时候用，例如百亿补贴、大促、日销、直播招商

4. 流程节点
   解决 Agent 工作流中哪里用，例如活动创建、选品、邀约、审核、复盘

5. 知识类型
   解决怎么用，例如规则、SOP、策略、案例、指标、工具说明

6. 风险与可信等级
   解决能不能用、用到什么程度
```

核心原则：

```text
业务域分类解决维护责任
专家 Agent 分类解决运行使用
流程节点分类解决 Agent 任务链路
知识类型分类决定 Agent 是严格执行还是策略推理
```

---

## 3.4 知识权限

### 目标

让 AI 能安全使用知识，不越权、不泄密、不乱执行。

### 管理权限和使用权限

权限分两类：

```text
1. 知识管理权限
   谁能新增、编辑、审核、发布、废弃知识

2. 知识使用权限
   谁能搜索、召回、引用、建议、执行
```

管理权限按：

```text
业务域 + 专家 Agent
```

来组织。  
PD 和业务负责人共同作为知识管理员，负责知识发布和审核。

### Agent 使用权限

使用权限是专家 Agent 的权限。  
PD 需要为每个 Agent 设计对应的 AI 角色权限，并接入现有权限体系：

```text
能访问什么数据
能调用什么工具
能使用什么知识
能给什么建议
哪些动作必须人工确认
哪些动作禁止执行
```

### 业务域知识是否给 Agent 使用

业务域知识不能无脑接入专家 Agent。

应由产品经理判断：

```text
这条业务域知识是否对当前 Agent 有用
是否符合当前任务场景
是否有风险
是否需要业务 owner 确认
```

产品经理要对 Agent 最终效果负责，因此需要决定 Agent 订阅哪些业务域知识。

### 商家版知识披露

商家和小二知识同源，但商家 Agent 不能直接使用小二内部知识。

```text
小二侧知识 -> 商家端使用
必须经过脱敏、外部化、披露流程
```

商家 Agent 只能使用：

```text
公开知识
商家可见知识
经过披露审核的外部化知识
```

### 用户主动操作 Agent 时的权限校验

当商家或小二主动操作 Agent 时，Agent 执行前必须校验：

```text
当前用户角色
数据权限
操作权限
任务范围
是否涉及用户无权限的数据或动作
```

Agent 不能因为自己有知识，就越过当前用户的业务权限。

---

## 3.5 知识使用

### 目标

让 Agent 在正确场景、正确权限、正确可信度下使用知识。

Agent 使用知识时需要判断：

```text
当前任务是什么
当前用户是谁
当前 Agent 是什么
当前业务对象是什么
知识是否有权限
知识是否最新
知识是否可信
知识是否适用
是否涉及高风险动作
```

Agent 输出时要能说明：

```text
依据来自哪条知识
是否为正式规则
是否需要人工确认
是否涉及审批
```

---

# 4. 组织共建机制

## 业务团队

负责：

```text
提供业务资料、SOP、规则、案例、复盘
参与 AI 主动学习访谈
确认关键知识是否正确
补充适用边界和例外情况
反馈 Agent 使用问题
```

## 产品团队

负责：

```text
定义 Agent 能力和业务流程
拆解每个流程节点需要的知识
设计知识分类、权限、更新机制
判断哪些业务域知识应接入 Agent
组织业务负责人确认关键知识
对 Agent 最终效果负责
```

## 技术团队

负责：

```text
建设经验中心和知识库平台
实现 AI 萃取、分类、搜索、权限、版本
实现 Agent 知识召回和使用链路
打通现有权限系统
记录使用日志和审计
支持任务回流和知识更新
```

一句话分工：

```text
业务负责经验正确性
产品负责知识产品化
技术负责平台化和自动化
```

---

# 5. 第一阶段里程碑

## 试点一：招商渠道运营托管项目

目标：

```text
利用招商 Agent 托管当前招商活动渠道运营
```

需要产出：

```text
招商 Agent 产品链路
招商活动创建流程
渠道准入规则
按目标 gap 选品策略
商家邀约策略
报名审核规则
异常商品巡检规则
低效商品汰换策略
AAR 复盘模板
招商 Agent 所需知识包
```

重点验证：

```text
Agent 能否基于目标和规则给出招商策略
能否托管渠道运营链路
能否在小二确认后推进执行
```

## 试点二：链接打爆项目

目标：

```text
围绕爆款链接数量 KPI，设计链接打爆链路
```

需要产出：

```text
链接打爆业务流程
爆款链接判断标准
商品诊断规则
流量/转化/价格/库存诊断树
链接优化策略库
活动匹配策略
供应链承接规则
低效链接汰换策略
链接打爆 Agent 知识包
```

重点验证：

```text
Agent 能否从 KPI 出发诊断链接问题
能否生成可执行的链接打爆策略
能否沉淀策略效果并持续迭代
```

---

# 6. 总结

这套知识库体系的核心不是“存文档”，而是建设一套：

```text
业务经验可获取
AI 可萃取
知识可治理
Agent 可使用
结果可回流
组织可共建
```

的 AI 经营知识底座。

最终目标是支撑超喵从：

```text
AI 辅助问答
```

升级为：

```text
AI 托管经营任务
```