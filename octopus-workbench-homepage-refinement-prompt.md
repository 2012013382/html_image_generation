# 任务：精修章鱼工作台首页，使其更克制、高级、统一

请基于当前项目已有首页直接修改，不要重新做一个页面。

当前首页已经有：

- 章鱼品牌 Hero
- 标题「懂猫超业务，才是你最好的工作搭档」
- Left / Center / Right 三种章鱼姿态及鼠标互动
- 极轻量 Hero 粒子 / 流星背景
- 新功能公告
- 任务动态
- 自定义区块能力
- 推荐能力 / 推荐问题
- 底部 AI Composer
- 当前数字人身份「章鱼·上进」

当前大结构基本正确，这次重点不是增加功能，而是通过**视觉层级、组件细节、空状态、对齐、颜色、间距和信息权重**提高整体品质。

最终希望达到：

**90% 克制成熟的 AI Productivity UI + 10% 章鱼自己的品牌人格。**

参考气质可以接近 WorkBuddy / Claude / Codex，但不要复制其视觉。

---

# 一、核心产品心智

必须始终保持以下产品逻辑：

```text
猫超小二
    ↓
章鱼数字人
    ↓
理解目标 / 拆解 / 规划 / 调度
    ↓
各类专家 Agent
```

用户前台主要面对的是：

**章鱼数字人。**

用户不应该被要求理解或主动选择背后的专家 Agent。

因此：

不要把：

```text
招商Agent
投流Agent
场景活动Agent
...
```

做成首页主要选择入口。

如果当前页面存在这样的 Agent Tab / Agent 标签，请改成：

```text
招商邀约
投流优化
活动运营
数据分析
商品诊断
...
```

即：

**用户选择的是“我要做什么”，不是“我要用哪个 Agent”。**

背后的专家 Agent 可以在任务执行过程中自然展示：

```text
招商专家正在处理
数据分析专家正在处理
```

但不要作为首页主导航。

---

# 二、「章鱼·上进」必须保留

输入框右下角保留：

```text
[章鱼头像] 章鱼·上进
```

这是“当前正在和我工作的数字人”的身份锚点。

大多数用户只有一个数字人，因此：

如果用户只有一个数字人：

```text
章鱼·上进
```

不显示 dropdown 箭头。

如果用户拥有多个数字人：

```text
章鱼·上进 ▾
```

才允许点击切换。

不要让 95% 只有一个数字人的用户看到一个无意义的下拉入口。

如果存在多个数字人，下拉列表可以：

```text
✓ 章鱼·上进
  场域运营

  章鱼·小满
  商家经营
```

名字下面允许有一行非常轻的职责描述。

---

# 三、首页整体视觉原则

页面上只能有一个真正有“体积感”的重组件：

**底部 AI Composer。**

其他区域全部克制。

不要让：

- Hero
- 公告
- 任务动态
- 推荐问题
- 自定义入口

每一块都变成 Card。

首页不要出现“卡片汤”。

当前任务动态区域已经是：

**无外围 Card、无整体边框**

这个方向是正确的。

**不要重新给任务动态加 Card。**

---

# 四、整体页面结构

保持大致：

```text
                章鱼 Hero

     懂猫超业务，才是你最好的工作搭档

            新功能公告


        主工作台内容区域


        业务能力 / 推荐动作


             AI Composer
```

首页内容最大宽度保持克制。

不要让主体内容横向撑满整个屏幕。

在 1440 / 1920 屏幕上：

主内容视觉宽度建议：

```text
880px ~ 1080px
```

当前如果已有合理容器，不要为了这个范围强行重构。

---

# 五、Hero 区域

当前章鱼 + 标题的方向是正确的。

保留：

```text
🐙
懂猫超业务，才是你最好的工作搭档
```

不要增加新的背景 Card。

不要再加新的装饰组件。

章鱼当前 Left / Center / Right 互动继续保留。

粒子 / 流星效果也继续保留，但必须保持极淡。

Hero 原则：

```text
第一眼：标题
第二眼：章鱼
第三眼：发现背景有一点动态
```

如果截图里第一眼看到粒子，说明效果过强。

Hero 标题建议：

```text
28px ~ 32px
font-weight: 600
line-height: 1.3
```

不要使用特别厚的 700 / 800 黑体。

---

# 六、新功能公告

当前 NEW 公告不要成为第二视觉中心。

目标是类似辅助信息：

```text
NEW   跨平台技能包支持一键导入   →
```

建议：

- 高度 32~40px
- 极浅背景或无背景
- NEW 可以保留品牌色
- 正文使用 secondary text
- 箭头非常轻

不要：

- 大面积绿色底
- 强 border
- 阴影
- 大圆角 Card

如果当前已经较轻，则只做细节精修，不要重做。

---

# 七、主工作台区域必须支持三种状态

这是这次最重要的结构规则。

同一个位置，根据用户状态展示不同内容。

但页面骨架和操作入口要保持稳定。

## 状态 A：全新用户

条件：

```text
无历史任务
无当前任务
无自定义区块
```

不要显示：

```text
任务动态 0
待处理 0
将要执行 0
```

也不要显示一个巨大的空白 Card。

改成：

```text
可以从这些开始                      + 添加区块

梳理本周值得重点招商的商家             →
生成一批商家邀约话术                   →
看看今天有哪些事情值得关注             →
```

可以有一句非常轻的说明：

```text
直接告诉章鱼你的目标，它会帮你拆解和执行
```

但不要做成长篇 onboarding。

这个区域的目标：

**帮助新用户完成第一次成功。**

---

## 状态 B：老用户，但当前没有需要处理的任务

不要把这种用户当成新用户。

显示：

```text
任务动态                            + 添加区块

今天暂时没有需要你处理的任务
有新的进展时，章鱼会放到这里

看看今天有什么值得关注 →
```

空状态保持极轻。

不要插大插画。

不要做空白大 Card。

---

## 状态 C：正常有任务用户

保留当前：

```text
任务动态

待处理 612      将要执行 0            全部任务 →

● 淘金币全网抵盘货邀约……
  还有 27 个商家待处理                 10:32

● 商品异常巡检……
  发现 12 个异常商品                   09:48
```

重点：

**任务动态当前无 Card、无外围边框，这个设计不要改。**

只精修：

- 字重
- 灰度
- spacing
- divider
- tab
- 时间
- 状态层级

---

# 八、「+ 添加区块」的统一位置

不要再用一整张：

```text
添加自定义区块
```

大卡片。

将它改成**工作台内容区域的轻量操作**。

无论新用户还是老用户，都放在主区域标题右侧。

例如：

```text
可以从这些开始                    + 添加区块
```

或：

```text
任务动态                         + 添加区块
```

样式：

```text
font-size: 13~14px
font-weight: 400~500
```

默认：

```text
#6B7280
```

hover 后：

```text
品牌青绿
```

不要：

- 彩色背景
- 大按钮
- 虚线框
- Card

用户点击后再打开配置流程。

---

# 九、自定义区块真正有内容以后

如果用户已经添加区块，可以在默认内容下方显示：

```text
我的关注

[秒杀活动监控]
[本周招商进度]
[库存异常商品]
```

这时候真正的自定义内容可以使用 Card。

但注意：

**Card 只用于承载真实内容，不用于承载“添加入口”。**

如果没有自定义区块：

不展示：

```text
我的关注
```

这种空 section。

---

# 十、能力标签

可以保留一个轻量区域：

```text
章鱼擅长

招商邀约   投流优化   活动运营   数据分析
```

这些是：

**业务能力 / 场景分类**

不是 Agent。

推荐使用 pill。

默认样式：

```css
background: #F7F8FA;
border: 1px solid #ECEEF1;
border-radius: 999px;
color: #525866;
```

hover：

```css
background: #F4FAF8;
border-color: #D9EEE8;
color: #137A67;
```

不要：

- 彩色实心 pill
- 每个标签一个不同颜色
- 大 icon
- 阴影

默认状态尽量无品牌色。

交互时才出现品牌色。

---

# 十一、推荐任务 / Prompt

推荐问题不要全部做成大 Card。

推荐使用：

```text
梳理本周值得重点招商的商家              →
生成一批商家邀约话术                    →
分析当前活动还有哪些增长机会            →
```

或非常轻量的 Row。

每行：

```text
44~48px
```

高度即可。

hover 时：

```text
background: 极浅灰 / 极浅青
```

不要：

- 大边框
- 大阴影
- 每行不同颜色

---

# 十二、AI Composer 是首页最重要的组件

这是页面唯一可以有明显“Surface”的组件。

推荐结构：

```text
┌───────────────────────────────────────────┐
│ 告诉章鱼你想完成什么……                      │
│                                           │
│ ＋附件   我的空间          章鱼·上进     ↑  │
└───────────────────────────────────────────┘
```

建议：

```css
background: #FFFFFF;
border: 1px solid #E7E9ED;
border-radius: 16px;
box-shadow: 0 1px 2px rgba(0,0,0,.025);
```

高度根据当前 Composer 功能保持合理。

不要大面积蓝紫渐变 border。

不要默认 glow。

Focus：

```css
border-color: #C8DDD8;

box-shadow:
  0 0 0 3px rgba(45,212,191,.05),
  0 4px 16px rgba(0,0,0,.035);
```

Focus 状态也必须非常轻。

发送按钮允许成为首页少数真正使用品牌色的 UI。

---

# 十三、Typography Design Tokens

全页减少字号种类。

推荐只保留：

```text
Hero title:
30px / 600

Section title:
16px / 600

Body:
14px / 400

Small action:
13px / 500

Metadata:
12px / 400
```

如果项目已有 Design Token，优先复用。

不要出现大量：

```text
13 / 14 / 15 / 16 / 17 / 18 / 20 / 22
```

不规则字号。

---

# 十四、文字颜色系统

建议：

```css
--text-primary: #17191E;
--text-secondary: #5E636C;
--text-tertiary: #9297A0;
--text-disabled: #C0C4CB;
```

页面里不要到处纯黑。

只有：

- Hero
- Section title
- 核心任务标题

使用 Primary。

说明、时间、状态等必须主动降级。

---

# 十五、边框系统

普通边框：

```css
--border-default: #E9EBEF;
--border-light: #F0F1F3;
```

Divider：

```css
#F0F1F3
```

不要使用：

```text
#D0D0D0
```

这种很明显的深灰边框。

---

# 十六、圆角体系

不要整个产品所有组件都是 20px 圆角。

推荐：

```text
Pill:
999px

Small button:
8px

Normal control:
10px

Content card:
12px

Composer:
16px

Large brand surface:
18px max
```

要有层级。

---

# 十七、Spacing System

尽量统一使用：

```text
4
8
12
16
24
32
48
64
```

不要出现大量：

```text
17px
21px
27px
31px
```

这种手工 spacing。

特别检查以下基线：

- Hero 左边缘
- 工作区标题左边缘
- 任务列表左边缘
- 能力区域左边缘
- Composer 左边缘

尽量形成统一垂直轴。

这是提高高级感非常重要的一点。

---

# 十八、任务动态内部精修

不改变当前无 Card 结构。

建议：

Section title：

```text
16px / 600
```

Tab：

```text
13~14px
```

非选中：

```text
#9297A0
```

选中：

```text
#202226
```

数量不要使用过大的彩色 Badge。

任务标题：

```text
14px / 500
```

任务说明：

```text
13px / 400 / secondary
```

时间：

```text
12px / tertiary
```

行 spacing：

```text
padding-block: 14~16px
```

分隔：

```text
1px #F0F1F3
```

不要整行 border box。

---

# 十九、Icon 统一

全部普通 UI Icon 使用同一种设计体系。

推荐：

```text
16px / 18px
stroke 1.5 ~ 1.75
line icon
```

不要在同一页面混：

- Emoji
- 实心 icon
- 彩色 icon
- 不同线宽 icon
- AI 生图 icon

**章鱼是页面唯一允许高度彩色、3D、丰富视觉的角色。**

普通 UI 必须极其克制。

---

# 二十、品牌色使用原则

章鱼本身已经非常彩色。

因此普通 UI 要减少品牌色。

品牌青绿主要用于：

- 当前状态
- hover
- focus
- CTA
- small highlight

不要：

```text
标题绿
Card 绿
Button 绿
Tab 绿
Icon 绿
Border 绿
```

同时全部出现。

页面 90% 依然应该由：

```text
白
黑
灰
```

构成。

---

# 二十一、Motion Design

当前章鱼 Left / Center / Right 切换是品牌互动，可以保留较有性格的动效。

其他 UI 全部克制。

推荐：

```text
普通 hover:
120~160ms

button:
120~160ms

popover:
160~220ms

section 展开:
180~240ms

章鱼切换:
350~450ms
```

不要：

- 普通按钮 bounce
- 卡片放大
- 每个元素 floating
- 无限循环动画

产品原则：

**只有章鱼互动有性格，普通 UI 非常职业。**

---

# 二十二、Hero 粒子特效

保留当前已有版本。

不要继续增加大量特效。

静态截图中：

用户不应该第一眼看到粒子。

动态时：

才能感觉页面有一点生命。

不要增加：

- 网格
- 扫描线
- 粒子网络
- 大 glow
- 星云
- 彩虹

---

# 二十三、Responsive

至少验证：

```text
1920
1440
1280
```

主内容不要随着屏幕无限变宽。

小屏时优先：

- 减少留白
- 缩小章鱼
- 缩小 Hero 上下间距

不要让标题因为章鱼而异常换行。

---

# 二十四、不要改动的内容

不要重做：

- Left / Center / Right 章鱼品牌素材
- 当前章鱼切换逻辑
- 整个导航结构
- 现有业务能力
- 任务数据逻辑
- Agent 后台调度逻辑

本次重点是：

**首页视觉系统和新老用户状态呈现。**

---

# 二十五、最终验收标准

第一眼应该是：

```text
一个成熟的 AI 工作台
```

第二眼：

```text
有一个很有记忆点的章鱼工作搭档
```

而不是：

```text
一个可爱的章鱼主题网页
```

页面应该做到：

**视觉重点少、层级清楚、边界轻、颜色克制、对齐严格、间距统一。**

对新用户：

第一屏应该自然告诉他：

**“我可以让章鱼帮我做什么。”**

对老用户：

第一屏应该自然告诉他：

**“现在有什么事情需要我关注。”**

用户不需要思考：

**“我应该选哪个专家 Agent？”**

用户只需要思考：

**“我想让章鱼帮我完成什么？”**

---

# 二十六、执行方式

请不要先大段输出设计分析。

直接：

1. 阅读当前首页组件与 Design Token；
2. 尽量复用已有组件；
3. 不大规模重构；
4. 优先通过 token / CSS / component states 精修；
5. 实现新用户 / 无任务老用户 / 有任务用户三种状态；
6. 调整自定义区块入口；
7. 调整业务能力表达；
8. 精修 AI Composer；
9. 启动项目；
10. 截图验收。

最后请提供：

- 新用户首页 1920px 截图
- 新用户首页 1440px 截图
- 有任务用户首页 1920px 截图
- 无任务老用户状态截图
- Composer Focus 状态截图
- `章鱼·上进` 单数字人状态
- 多数字人 dropdown 状态
- 修改文件列表
- 新增 / 修改的 Design Token
- 关键 spacing / typography / radius 参数

**如果某处为了“设计感”新增了不必要的 Card、颜色、阴影或装饰，请优先删掉，而不是继续美化它。**

